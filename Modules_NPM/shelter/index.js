const blue = require('./blue');
const sadie = require('./sadie');
const janet = require('./janet');

const allCats = [blue, sadie, janet];
// console.log(allCats); // Output: [ { name: 'blue', color: 'blue' }, { name: 'sadie', color: 'black' }, { name: 'janet', color: 'orange' } ]


module.exports = allCats; // Exporting the 'allCats' object 